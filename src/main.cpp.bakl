#include <iostream>
#include <stdlib.h>
#include "SDL2/SDL.h"
using namespace std;

#undef main
int main(int argc, char *argv[])
{
	SDL_Init(SDL_INIT_EVERYTHING);
	cout << "hello world!" << endl;
	atexit(SDL_Quit);
	return 0;
}